// Props for the component
export type WebcamFileLoaderProps = {
    // Put here the WebcamFileLoaderProps. For example:
    exampleProp: string;
};

// Props for the hook
export type UseWebcamFileLoaderProp = {
    // Put here the UseWebcamFileLoaderProp. For example:
    exampleProp: string;
};
